class Solution:
    def search(self, nums: List[int], target: int) -> int:
        #  problems states we are given an array that is sorted but its then rotated at a random pivot, our job is to return Tr
